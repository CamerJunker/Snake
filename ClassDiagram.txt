@startuml
MainApp ..|> GameView
GameView ..|> GameController
GameView ..|> GameModel

class MainApp {
    -- Fields --
    -{static}GameView gameView
    -{static}MainApp mainapp 
    -- Functions --
    +{static}main()
    +{static}startGame(int n, int m, GameView gameView, MainApp mainApp)
}

class GameView < extends JFrame implements ActionListener > {
    -- Fields --
    -GameController controller
    -SnakePanel panel
    -Timer timer
    -Timer renderTimer
    -int DELAY
    -int RENDER_DELAY
    -JButton MenuButton
    -PopupMenu popupMenu
    -MainAp mainApp
    -- Functions --
    +GameView(int n, int m, MainApp main)
    ..Implemented Function..
    +actionPerformed(ActionEvent e)
    .. GUI Functions ..
    +ClosePopupMenu()
    +CloseGameView()
    +restartGame()
    +toggleMenu()
    .. Setter ..
    +setDifficulty(Difficulty difficulty)
}

class GameController < extends KeyAdapter > {
    -- Fields --
    -GameModel model
    -Timer timer
    -int baseDelayMs
    -GameView view
    -SoundManager soundManager
    -Direction requestedDirection
    -int lastScore
    -int speedupMs
    -{static} int MIN_DELAY_MS
    -- Functions --
    +GameController(GameModel model, SnakePanel panel, Timer timer, GameView view)
    .. Implemented Functions ..
    +keyPressed(KeyEvent e)
    .. Game Functions ..
    +move()
    +pause()
    +unPause()
    +resetGame()
    .. Setter ..
    +setDifficulty()
    .. Getter ..
    +getModel()
}

@enduml